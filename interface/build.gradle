bootRun {
    enabled = false
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
dependencies {
    api group: 'org.springframework.boot', name: 'spring-boot-starter-validation'
    api project(":repository")
}
